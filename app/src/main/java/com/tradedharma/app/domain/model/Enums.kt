package com.tradedharma.app.domain.model

enum class InstrumentType { EQUITY, FUTURES, OPTIONS, OTHER }
enum class TradeDirection { BUY, SELL }
enum class OptionType { CALL, PUT }

enum class ReviewRating { YES, PARTIAL, NO }
